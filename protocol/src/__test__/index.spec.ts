/**
 * @file This file exists only to satisfy the test command, and avoid making code for exceptions in pipeline.
 */

import test from "ava";

test("This only exists to avoid cluttering pipelines too much", (t) =>
  t.pass());
